import numpy as np

#1. 시뮬레이션 환경 설정
GRID_SIZE=200 #가로세로 픽셀 수
SIM_WIDTH=2.0 #시뮬레이션 공간의 실제 너비 (단위: 미터)
DX=SIM_WIDTH/GRID_SIZE #한 픽셀의 실제 길이
X=np.linspace(0, SIM_WIDTH, GRID_SIZE)
Y=np.linspace(0, SIM_WIDTH, GRID_SIZE)
XX, YY=np.meshgrid(X, Y) #2D 그리드 생성

#2. 파동 파라미터(사용자 입력 부분)
